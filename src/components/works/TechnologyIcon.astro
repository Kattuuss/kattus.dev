---
import { Icon } from "astro-icon/components";

const { source, name } = Astro.props;
---

<div class="flex items-center gap-2">
    <Icon name={source} class="text-2xl" />
    <span class="font-bold">{name}</span>
</div>